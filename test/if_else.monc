// TODO: This is a basic test for parsing. Need more tests for functionality.

int test(int arg) {
    if (arg == 42) {
        return 1;
    } else {
        return 2;
    }
    return 3;
}

int main() {
    test(42);
    return 0;
}
