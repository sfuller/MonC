
// TODO: Remove the fail_ prefix once continue statements are supported.

int main() {
    int x = 0;
    int y = 0;
    for (int i = 0; i < 4; i = i + 1) {
        x = x + 1;
        continue;
        y = y + 1;
    }
    return x == 4 && y == 0;
}
