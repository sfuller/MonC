name: ci

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Build and Test via Docker
      uses: docker/build-push-action@v1.1.1
      with:
        push: false
